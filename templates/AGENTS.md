# AGENTS.md — Research Constitution

> **用途**：放在科研项目根目录，由项目所有者维护，Agent 只读。
> **安装**：复制为项目根目录的 `AGENTS.md`，按需删除不适用章节。
> **优先级**：文档冲突时以本文件为过程政策；科学事实以实验证据为准。

---

## Research Objective

- 项目目标：<在此填写>。
- 非目标：<在此填写>。
- 成功判据：<在此填写>。
- 优化目标是**有意义地降低不确定性**，不是实验数量、文档完整度或局部指标改善。

## Toolchain and Key Constraints

- 语言 / 编译器 / 版本：<在此填写>。
- 关键约束（性能、平台、依赖等）：<在此填写>。

## Experiment Discipline

- 每个实验绑定一个主假设和当前主矛盾。
- 看结果之前，先记录 baseline、预期性能区间、预期机制信号和各结果的对应动作。
- 每次运行后比较 baseline、预期、实际，并先定位哪条因果边断裂（实验有效性 → 干预 → 机制 → 目标 → 价值），再提修改方案。
- 下一个实验必须降低一个决策相关的不确定性；说明不了"改变了哪个判断"就不开跑。
- 新想法进停放区，除非它推翻当前问题或改变当前决策，否则不进入主线。
- 局部异常只有在能解释主要偏差时，才能取代主矛盾。
- 连续两轮没有缩小决策相关的不确定性，停止实验序列并重新界定问题。

## Source Of Truth

1. `AGENTS.md`：本文件，过程政策。
2. `.research/global.md`：方向索引与指针（不含目标与规则）。
3. 方向 `00-overview.md`：当前状态与主矛盾（只记现状，不堆历史）。
4. 已登记实验计划：冻结的假设与预期。
5. 运行报告：观测证据与判断。
6. 文献笔记：外部证据。
7. 生成的状态快照：一次性视图，不作依据。

## Context Discipline

- 会话开始只读 `.research/global.md` 和目标方向的 `00-overview.md`，按需跟随链接。
- 不整树加载 `.research/`；归档和 superseded 文档默认不活跃，除非点名。
- 不从旧报告推断当前政策。

<!-- 以下章节按需保留，与科研项目无关时删除 -->

## Git Discipline (optional)

- commit、删分支、合并需显式授权。
- 实验分支永不直接合并主线；成功方向走 `promote/NN-slug`。
- 大产物不进 Git，走外部 manifest 追踪。

## Output Discipline (optional)

- 先说结论，再给依据和下一步；不复述收尾。
- 外部事实附来源；不确定的写"尚未验证"或"我推测"。
- 一段能说清就不用表格；独立要点用列表；只有横向比较才用表格。
